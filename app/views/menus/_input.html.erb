<div class="w-full">
    <%= yield%>
    <div class="text-red-600 mt-1 min-h-6">
        <% if local_model.errors[field].any?%>
            <%= local_model.errors[field].first%>
        <% end %>
    </div>
</div>